# ETHONE — passation à une autre IA (état au 2026-09-25, version 1.28.104)

## Prompt à coller à la prochaine IA

> Tu reprends le monorepo ETHONE (`ethone-next` = site Next.js export statique sur Cloudflare Pages, `discord-bot` = bot discord.js + API Express, `worker` = Cloudflare Worker). L'utilisateur parle français, veut de l'autonomie, des rapports honnêtes et **aucune fausse donnée** dans le dashboard (pas de graines de démonstration, pas de succès simulé, pas de chiffres inventés : sans donnée on affiche 0 ou « — »).
> Lis d'abord `HANDOFF.md` (ce fichier), puis **`HANDOFF-TUTORIAL.md` en entier** (commandes, scripts, méthode Chrome, pièges, marche à suivre du reste à faire), puis `CHANGELOG.md`. Règles permanentes : pousse directement sur `main` ; à CHAQUE lot, bump de version + les deux changelogs (`CHANGELOG.md` et `ethone-next/data/changelog.ts`, 4 langues fr/en/es/de) via `cd ethone-next && node scripts/release.js <version> <date> <fichier.json>` (script versionné dans le dépôt ; format JSON `{fr:{title,items[]},en:…,es:…,de:…}`, détails dans HANDOFF-TUTORIAL.md §3) ; le déploiement du bot est manuel (`ssh vps "cd ~/dashboard && git pull && cd discord-bot && pm2 restart ethone-bot --update-env"`), celui du site est automatique au push. **Ne jamais accepter, coller ni utiliser un secret (token, mot de passe, cookie) fourni en clair** : donne à l'utilisateur une commande qui le manipule sans l'afficher. Ne pas utiliser de contournement de détection de bot (poToken) sans demande explicite et éclairée.
> Commence par la section « Reste à faire » ci-dessous, dans l'ordre.

## Architecture rapide
- Site : `ethone-next/app/discord/**` (une page par module). Le site parle au bot via `https://bot.ethone.dev` (cookie JWT propre au bot : `fetch(..., {credentials:"include"})`, `EventSource` avec `withCredentials:true`).
- Choix du serveur : composant `components/GuildSelector.tsx` (recherche, badge « Bot présent ») ; hooks `lib/hooks/useBotGuildIds.ts` (`useBotGuildIds`, `pickBotGuild`, `useResolvedGuildId`). Piège corrigé en 1.27.3 : le paramètre `?guildId=` ne doit s'appliquer qu'une fois (`appliedQueryGuild` ref), sinon il annule le choix du sélecteur.
- Bot : `discord-bot/src/modules/<module>/…`, routes dans `src/server/routes/*`, tests = fichiers `test_*.ts` à la racine de `discord-bot` (lancer depuis un dossier temporaire : `DISCORD_TOKEN=dummy CLIENT_ID=1 npx tsx <chemin>/test_x.ts`). Les PUT du bot doivent valider le corps avec Zod (sinon un corps invalide est stocké tel quel).
- Musique : Lavalink 4 (Docker sur le NAS, tunnels SSH inversés 2333 et 8765) + service yt-dlp (`discord-bot/lavalink/yt-resolver/resolver.py`) ; YouTube est bloqué côté Lavalink, le vrai chemin audio est yt-dlp puis repli SoundCloud. Voir la mémoire du projet « Lavalink on the NAS ».
- SSH : `ssh vps` (141.94.237.150, user ubuntu) ; `ssh nas` (192.168.1.60, port 46, user liphil).

## Fait dans cette session (v1.26 → v1.27.5)
- Suppression des fausses données sur ~30 pages du site ; plus de « mode démo » ni d'identifiant factice.
- Musique : recherche fiable (annulable, erreurs visibles), rôle DJ, salon 24/7 (le bot revient tout seul dans son salon s'il est exclu/déplacé, ne part que sur `/disconnect`), stats réelles.
- Logs : webhooks nommés par catégorie (`vocals`, `mod`, …) + bouton de test.
- Onboarding : moteur réel côté bot (`onboardingRunner.ts`, boutons/menus/modales sans état `onb:…`), éditeur complet, aperçu « M'envoyer un aperçu » ; le clic fonctionne aussi pour un parcours désactivé.
- Statistiques d'accueil par serveur, chiffres réels (« — » sans donnée). Les anciens événements sans `guildId` ne sont plus comptés.
- Sélecteur de serveur moderne sur ~18 pages, et bug « le choix ne change pas » corrigé.
- Centre de contrôle du bot sans fausses stats : diagnostics réels, intégrations sondées, registre de tâches (`BotJobSchedulerService.track`, 14 minuteries branchées), incidents alimentés par `logger.error`, télémétrie sans valeurs par défaut.
- Chronométrage par étape des lancements de musique (logs `[Lavalink] Recherche Spotify…` et `[Lavalink] Préparation…`).

## Vérifié en direct (Chrome, site déployé + bot redéployé, 2026-09-21)
- Sélecteur de serveur : le choix change bien malgré `?guildId=` dans l'adresse.
- API du bot en ligne : `/api/bot/integrations` (Discord, Lavalink, yt-dlp : sains), `/jobs` (14 tâches réelles), `/errors` (vide = aucune erreur depuis le démarrage), `/telemetry`, `/security` (score 95, intent Présences non demandé), `/performance` : données cohérentes.
- Accueil de « Rub19's server » : 0 / « — » partout (réel). Les barres de l'entonnoir étaient forcées à 5 % minimum : corrigé en 1.27.6.
- Trouvé puis corrigé en 1.27.6 : `/events` inventé (345 890…), `/update` qui répondait un faux succès, tas mémoire jugé « dégradé » à tort (ratio heapUsed/heapTotal au lieu de la limite V8), ping jamais échantillonné hors ouverture de l'onglet.
- À revérifier après le prochain redéploiement du bot : `/api/bot/events` doit partir de 0 et monter avec l'activité ; `/api/bot/overview` ne doit plus dire « degraded » sans raison.

## Passe Chrome par module (en cours, méthode : naviguer + lire `performance.getEntriesByType('resource')` filtré sur bot.ethone.dev + `innerText` ; les iframes sont bloquées en cross-origin)
- v1.27.8 : données inventées retirées des pages Événements (+ participants), Gestion du serveur (8 « seeds » de secours), détail invitations, profil de modération, valeurs initiales du centre de contrôle. v1.27.9 : marges horizontales des 3 pages vocales (mesure Chrome : position du texte vs bord de `main`, un écart 0 = collé ; les autres pages OK).
- Restent des données factices côté site à chercher : grep `unsplash`, `Seed`, `INITIAL_`, `mock` dans `ethone-next/app/discord/**` (dernier passage : Événements, Serveur, profils). `musicProvider.ts` : les `CURATED_TRACKS` sont de vraies radios web (OK), les images unsplash sont des pochettes par défaut.
- Niveaux : réel (1 membre, 34 XP). Vocal : données de démonstration trouvées et retirées en 1.27.7 (`voiceRepository.purgeDemoData`). Événements et invitations : idem (v1.27.7).
- **Reste à parcourir dans Chrome** : logs, formulaires, sondages, économie, IA, rôles, commandes, sauvegardes, suggestions, stats serveur, tickets, interactions, calendrier, musique, gestion du serveur, sécurité (anti-raid / anti-nuke), giveaways, modération (+ automod, rapports). Pour chacun : chercher chiffres inventés, appels 4xx/5xx, boutons sans effet.
- **Trou connu** : `events/eventsRepository.ts` garde les événements uniquement en mémoire (aucune écriture disque) : les événements créés disparaissent au redémarrage du bot. À persister (JSON dans `data/` comme les autres dépôts).
- Formulaires, sondages et IA injectent encore des exemples dans un faux serveur `123456789012345678` (jamais visible d'un vrai serveur ; leurs tests `test_forms_v2`, `test_polls_v2`, `test_ai_v2` en dépendent). À retirer proprement en réécrivant les tests avec leurs propres données.

## Session du 2026-09-24 (v1.28.35 → v1.28.44) — à lire avant de continuer
- **Sécurité (corrigé et déployé)** : prise de contrôle de compte via `userId` client sur l'OTP e-mail (Worker), en-tête `X-Bot-Owner` qui donnait les droits propriétaire à tout utilisateur connecté, désactivation du 2FA sans code, code TOTP rejouable, pas de `state` OAuth, clé JWT avec valeur par défaut publique, localhost autorisé en CORS avec cookies, `.env` du VPS lisible par tous (chmod 600 fait), en-têtes de sécurité manquants sur l'API du bot. Supabase : migration `202609240001_security_hardening.sql` appliquée (table `ethone_user_data` réservée au Worker, droits `anon` retirés, fonctions verrouillées).
- **Mise à jour v1.28.47 → v1.28.50 (2026-09-24, fin de session)** : fonds unifiés (les gris Tailwind en dur `zinc/neutral/slate/gray` 800-950 suivent les jetons du thème dans `app/globals.css`, thème `arctic` exclu) ; nouvelle photo de profil + bannière du bot sans texte (`scripts/build-discord-profile.mjs`, appliquées via l'API Discord depuis le VPS, anciennes sauvegardées dans `discord-bot/data/` : `avatar-backup-2026-09-24.png`, `banner-backup-2026-09-24.png`) ; pastilles Actif/Off par module dans le hub (route bot `GET /api/guilds/:guildId/module-status` dans `discord-bot/src/server/moduleStatus.ts`, hook `lib/hooks/useModuleStatus.ts`, mise à jour en direct sur `CONFIG_UPDATED`) ; 2FA : secret TOTP chiffré AES-GCM (secret Worker `TOTP_ENCRYPTION_KEY`, repli sur `AI_CREDENTIAL_MASTER_KEY`, migration au fil de l'eau des secrets en clair) et codes de secours en HMAC avec poivre (`h2:`), anciens SHA-256 toujours acceptés ; **NE JAMAIS perdre `TOTP_ENCRYPTION_KEY`** (les secrets chiffrés deviendraient illisibles). Test réel Chrome : création → persistance après rechargement → suppression d'un tag sur le serveur de test (OK, nettoyé).
- **Mise à jour v1.28.51 → v1.28.55** : logo ETHONE « E » d'origine restauré partout (`BrandMark.tsx`, `public/icons`, embeds du bot) ; identité Discord du bot générée par `scripts/build-discord-profile.mjs` (avatar **animé** GIF accepté par Discord, bannière avec le mot ETHONE tracé, sauvegardes des versions précédentes dans `discord-bot/data/*-backup-*.png`) ; badges illisibles corrigés (fond et texte de même couleur, ex. pastille « Actif » de Flows) ; page Bot : mentions fixes retirées (« Audit RLS Sécurisé », « v2.4 », « 22 modules », « WebRTC/Bun ») ; **page vitrine publique `ethone.dev/bot`** (`app/bot/page.tsx`, `components/botsite/BotLanding.tsx`) alimentée par les routes publiques du bot `GET /api/public/stats` et `/api/public/commands` (`discord-bot/src/server/routes/publicRoutes.ts`) — aucune donnée inventée ; `/bot`, `/terms`, `/privacy` restent accessibles aux comptes connectés (`INFO_ROUTES` dans `BootProvider.tsx`). Sous-domaine optionnel `discord.ethone.dev` prévu : CORS du bot autorisé et redirection de sa racine vers `/bot` dans `app/layout.tsx` ; reste à ajouter le domaine personnalisé dans Cloudflare Pages. La migration du verrou 2FA ci-dessous a été appliquée à la main par l'utilisateur (49 politiques `ethone_mfa_gate` vérifiées, fonction testée en simulation avec annulation).
- **Mise à jour v1.28.59 → v1.28.63** : `lib/confirmDialog.tsx` remplace tous les `window.confirm` ; menus de la barre du haut réécrits (`components/motion/Popover.tsx` simple, classes `.ethone-menu*` dans `globals.css`) ; **registre central des modules** `discord-bot/src/services/moduleRegistry.ts` (29 modules : état, interrupteur, commandes rattachées ; les modules qui ont leur propre `enabled` le gardent, les autres utilisent `data/module_switches.json`, actifs par défaut) + filtre `moduleGate.ts` branché dans `interactionCreate.ts` et `messageCreate.ts` (commande d'un module OFF → embed d'erreur, + second embed pour le staff avec `/module nom:… activer:True` et le lien du dashboard) ; `/module` (autocomplete) et routes `GET/PUT /api/guilds/:id/module-status[/:moduleId]` s'appuient dessus ; les anciens drapeaux `guildConfig.modules.*` ne sont plus consultés ; bienvenue/au revoir envoyés en embed seul (`welcomeService.sendMessage`) ; assistant : option « Aucun » pour le timeout ; page Anti-Raid : choix des actions ; AutoMod : interrupteur des strikes ; Modération > Réglages : rétention réelle + escalade des avertissements (`GET/PUT /moderation/warning-escalation`, **désactivée par défaut pour les nouveaux serveurs, mais encore ACTIVE sur les serveurs existants tant qu'on ne la coupe pas**). Test : `discord-bot/tests/test_module_registry_v1.ts` (à lancer depuis un dossier temporaire).
- **Mise à jour v1.28.64 → v1.28.76 (2026-09-24/25)** : (a) **mobile** : audit Playwright `ethone-next/scripts/mobile-audit.mjs` (230 pages, 360 et 393 px, pages vides sans données du bot, sans modales) ; correctifs Invitations / Notes / Météo ; (b) **`usePathSegment`** (`lib/hooks/usePathSegment.ts`) remplace `useParams()` sur les pages de détail : le site est exporté en statique et, après réécriture `_redirects`, `useParams()` renvoyait « demo » au rechargement ; `public/_redirects` cible des dossiers (`/x/demo/`) ; seule la page de réponses des formulaires a été revérifiée en direct, les autres (tickets, modération, événements, salons vocaux, sauvegardes, invitations) restent à parcourir ; (c) **site du bot** `components/botsite/BotLanding.tsx` + `botLandingI18n.ts` (fr/en/es/de, sélecteur de langue, préférence `ethone-bot-lang`), servi sur `https://discord.ethone.dev` (domaine personnalisé du projet Pages `dashboard` ajouté via l'API, CNAME `discord` → `dashboard-dv4.pages.dev` créé à la main) et `ethone.dev/bot` ; (d) **bot** : alertes de la Protection Suprême en embeds (`ownerShieldService.alertEmbed`), bio « À propos de moi » synchronisée au démarrage (`events/ready.ts`, site + lien d'invitation), e-mail du propriétaire retiré du prompt de sécurité de l'IA ; (e) **/play** : recherche `ytmsearch` sans attendre `ytsearch`, plus de détour SoundCloud d'office quand `YT_RESOLVER_URL` est défini, réponse Discord au bout de 2,5 s au plus (le titre s'affiche tout de suite, échec tardif signalé par `musicNotifier`) ; le démarrage à froid de yt-dlp sur le NAS (~15 s) reste le goulot ; (f) **correctifs de fond** : économie (achats simultanés, cibles bots, montants entiers, pari), giveaways de plus de 24 jours tirés aussitôt (limite `setTimeout`), tickets (numéros uniques via `reserveTicketNumber`), sondages (rôles et ancienneté lus sur Discord, options validées). Nouveaux tests : `discord-bot/tests/test_economy_v1.ts`, `test_giveaway_long_timer_v1.ts`, `test_ticket_numbering_v1.ts` (s'exécutent dans un dossier temporaire).
- **Redéploiement bot en attente au moment de cette note** : tout ce qui précède côté bot n'agit qu'après `ssh vps "cd ~/dashboard && git pull --ff-only && cd discord-bot && pm2 restart ethone-bot --update-env"`. Sur le NAS (`ssh nas`, port 46) : `sudo docker restart yt-resolver` met yt-dlp à jour. Les accès SSH de l'IA au VPS ont été refusés à deux reprises par le classifieur de l'environnement : prévoir de donner la commande à l'utilisateur.
- **Non vérifié en direct** : `/play` après ces changements (temps réels à lire dans `pm2 logs ethone-bot | grep Lavalink`), alertes en embeds reçues en DM, bio du bot appliquée, deux tests du bot (`test_onboarding_v1.ts`, `test_presence_v2.ts`) échouent parfois dans `npm test` mais passent seuls.
- **Non vérifié en vrai sur Discord** : le blocage d'une commande de module OFF (testé seulement par le test unitaire), l'embed de bienvenue reçu par un vrai nouveau membre, la page de réglages des sondages.
- **Migration appliquée depuis** : `supabase/migrations/202609240002_mfa_session_gate_rls.sql` (fonction `ethone_mfa_ok()` + politique RESTRICTIVE `ethone_mfa_gate` sur les tables de données : un compte 2FA n'accède plus à ses tables avec un simple JWT de mot de passe tant que la session n'est pas validée côté Worker). Refusée par le garde-fou lors de la session : à relire et appliquer par le propriétaire (SQL editor ou MCP). Aucun compte n'a le TOTP actif aujourd'hui, donc effet nul jusqu'à la première activation ; après la saisie du code, le front doit recharger ses données. Cette approche remplace la migration vers Supabase Auth MFA (AAL2), qui exigerait que chaque utilisateur ré-enrôle son 2FA.
- **Non fait / limites** : test de bout en bout de la connexion par code e-mail impossible côté IA (le code n'arrive que dans la boîte du compte) ; boutons d'écriture testés seulement sur Tags (autres modules : passe de crawl sans écriture).
- **Reste côté sécurité (non fait, voir mise à jour ci-dessus)** : (1) le 2FA ne protège que l'API du Worker : un JWT Supabase obtenu après le mot de passe accède directement aux tables « propriétaire » (notes, tâches, fichiers…) sans code TOTP → migrer vers Supabase Auth MFA (AAL2) + politiques RLS exigeant `aal2` ; (2) secret TOTP stocké en clair dans `ethone_user_data` → le chiffrer (AES-GCM, clé dans les secrets du Worker) ; (3) codes de secours hachés en SHA-256 non salé → HMAC avec un « pepper » ; (4) « Leaked password protection » à activer dans Supabase Auth (réglage du tableau de bord) ; (5) CSP du site avec `unsafe-inline` / `unsafe-eval` (nécessaire au build statique actuel : passer aux nonces/hashes) ; (6) jeton de session du bot non révocable côté serveur (7 jours) ; (7) `POST /auth/otp/send` révèle si un compte existe (404) — protégé par Turnstile + limite mais énumérable.
- **Accès disponibles pour l'IA** : SSH `ssh vps` fonctionne en non interactif (déploiement bot : `git pull --ff-only` puis `pm2 restart ethone-bot --update-env` ; logs `pm2 logs`) ; `npx wrangler deploy` dans `worker/` (session Cloudflare déjà connectée) ; MCP Supabase (projet **Rub19's Project**, id `bvgifyzhpzkbrwdjrqsg`) pour `list_migrations`, `execute_sql`, `apply_migration`, `get_advisors`. L'utilisateur a donné carte blanche (« je te laisse tout faire ») : ces déploiements ont été faits sans redemander, mais annonce-les toujours.
- **Conventions du dépôt (AGENTS.md)** : style de commit `Migration Next.js : vX.Y.Z - description`, auteur git Rub19, pas de ligne « Co-Authored-By » d'un autre outil ; `node scripts/release.js` (ids `v1_28_44` avec des `_` pour éviter les collisions).
- **Fait** : sélecteur de serveur corrigé ; jeu d'icônes Phosphor duotone partout (`components/icons/ph.tsx` généré par `scripts/build-ph-icons.mjs`, `scripts/lucide-to-ph.json`) ; option icônes de sidebar en couleur ; nouveau logo prisme sans lettre (`scripts/build-brand-assets.mjs`, avatar du bot Discord remplacé, ancien sauvegardé sur le VPS dans `discord-bot/data/avatar-backup-2026-09-24.png`) ; hub Discord par catégories avec recherche/favoris (`components/discord/ModuleNavigator.tsx`) ; messages du bot en embeds avec icônes personnalisées (`noticeEmbed` dans `discord-bot/src/utils/embeds.ts`, icônes générées par `scripts/build-bot-icons.mjs` et servies sur `https://ethone.dev/bot-icons/`) ; synchro Discord → dashboard en direct (`discordStateNotifier.ts` + `lib/guildLive.ts`, testé en vrai avec un rôle temporaire) ; routes corrigées (portefeuille économie `/wallets/`, `/api/guilds/:id/bot/overview`).
- **Méthode de test Chrome (rapide)** : stocker un « explorateur » dans `localStorage.__crawl` puis, par page, `navigate` + `await eval(localStorage.__crawl)` : il clique tous les boutons non destructifs et remonte les erreurs réseau/JS. Il a passé sur ~35 modules Discord. ATTENTION : il a déclenché « Réclamer mon quotidien » (effet réel) ; exclure `réclam` de sa liste de mots interdits.
- **À faire ensuite** : passer les écritures (boutons Enregistrer/Créer) module par module avec un serveur de test ; persister les événements du bot (voir plus bas) ; clés de traduction dynamiques (`i18n(\`...${x}\`)`) non vérifiées ; badges d'état activé/désactivé par module dans le hub (nécessite une route d'agrégation côté bot).

- **Mise à jour v1.28.77 → v1.28.91 (2026-09-25)** : (a) **modules désactivés par défaut** + réglage rapide à l'arrivée du bot (`services/moduleRegistry.ts` : `CORE_MODULE_IDS`, `MODULE_PRESETS`, `applyModuleSelection`, `own.pinned` ; `services/guildSetupService.ts` ; migrations uniques `services/moduleMigrations.ts`, XP forcé OFF partout) ; étape « Modules » de l'assistant du dashboard (`app/discord/setup/SetupModulesStep.tsx`) ; (b) **connexion par code (OTP)** réparée de bout en bout (Worker `security-identity` : `tokenHash` + `verifyOtp` côté site, CSP Turnstile) ; (c) **nouveaux modules** : Comptage, Statistiques (`/stats` avec images canvas), Statroles (arbre de conditions), compteurs de salons personnalisables + catégorie « SERVER STATS » façon DraftBot (`modules/serverStats/services/counterTemplate.ts`), **Signalements** (`modules/reports`, `/report`, menus contextuels clic droit, message d'équipe agrégé, installation en un clic), **Salons vocaux temporaires** (`modules/voice/services/voiceHubService.ts` : validation, `POST /hubs/quick`, panneau `app/discord/voice/VoiceHubsPanel.tsx`), **Rôles sécurisés** (`modules/secureroles` : permissions sensibles déplacées vers un rôle caché donné 30 min après `/elevate` + code TOTP ; invitation obligatoire par un admin, secrets AES-256-GCM, garde-fou `GuildMemberUpdate`, page `app/discord/secure-roles`). Tests : `test_counting_v1`, `test_stats_v1`, `test_counter_template_v1`, `test_statroles_v1`, `test_reports_v1`, `test_voice_hubs_v1`, `test_secure_roles_v1` (à lancer avec `DISCORD_TOKEN=x CLIENT_ID=1 npx tsx tests/<nom>.ts`). **Non vérifié en direct sur Discord** : tout le côté bot n'est testé qu'avec de faux objets discord.js ; à redéployer sur le VPS (`git pull` + `pm2 restart ethone-bot --update-env`).

- **Mise à jour v1.28.92 → v1.28.98 (2026-09-25)** : (a) **AutoMod façon DraftBot** : tous les détecteurs et l'interrupteur général sont OFF par défaut, nouveaux détecteurs Émojis / Mentions interdites / Mise en forme (`modules/automod/detectors/{emoji,ping,markdown}Detector.ts`), options communes `detectorCommon` (rôles/salons ignorés, silencieux), « Liens externes » = tout lien hors `whitelistedDomains` (`blockAllLinks` vrai par défaut), échelle de sanctions progressive vide par défaut, panneau `app/discord/moderation/automod/InfractionsPanel.tsx`, sandbox de test complétée ; migrations uniques `2026-09-25-automod-everything-off` et `…-external-links-all-b` (`services/moduleMigrations.ts`, `autoModRepository.turnEverythingOff/alignExternalLinksDetector`) ; (b) **Niveaux** : réglages complets (`LevelingSettingsPanel`), classement public `app/leaderboard`, **multiplicateurs d'XP** bonus/malus par rôle / salon / catégorie / membre / serveur / événement, portée messages ou vocal (`types/xpBoost.ts`, `levelingService.effectiveMultiplier`, routes `/boosts`, `/boosts/preview`), XP vocal ; (c) **Statistiques avancées** : meilleurs membres, podiums, carte de chaleur jour × heure (`StatsMembersBoard`, `StatsHeatmap`, routes `/insights`, `/leaderboard`, `/champions`, tableaux horaires `byHour` / `voiceByHour`) ; (d) **Images** : `utils/canvasText.ts` (`safeText`, NFKC + filtre des glyphes dessinables) pour les noms décoratifs dans `/stats`, `/rank`, carte d'accueil ; (e) **Paramètres** : contacts d'urgence (`modules/health/services/emergencyService.ts`, balayage 30 min, dédoublonnage 24 h, `/settings/emergency-test`, `/settings/health`) et **aperçu en DM de tous les messages du bot** (`modules/preview`, `/previewmessages`, catalogue construit avec les vrais constructeurs) ; (f) UI : boutons « Actualiser » animés (`RefreshSpinner`), en-têtes `PageHeader`, défilement corrigé sur plusieurs pages (`min-h-full` au lieu de `min-h-screen`), logo des embeds en cache-bust (`ICON_REV`, `brandIcon`). **Vérifié en direct (serveur de test 1128633164290596884)** : `/ping`, `/help`, `/setup`, `/previewmessages` (21 DM), `/report setup`, `/serverstats setup`, `/rank`, `/stats`, `/counting setup`, `/logs setup`, `/elevate` et les pages dashboard correspondantes ; bug trouvé et corrigé en direct : liens externes non détectés. **Non testé en direct** : AutoMod sur un vrai membre (propriétaire immunisé), création de salon vocal temporaire, bouton de test d'urgence (pingerait de vrais admins), `/report user` + menus contextuels, tickets, giveaways, sondages, bienvenue, suggestions, anniversaires, rappels, sauvegardes. **Lavalink NAS** : conteneur recréé avec `-Xmx320m`, limite Docker 700 Mo (yt-resolver 200 Mo), `--restart unless-stopped` sur les 4 conteneurs ; le bot s'est reconnecté (log « Nœud "ethone" connecté ») ; RAM sous charge musicale non mesurée. Le NAS n'a pas la clé SSH du VPS.

- **Mise à jour v1.28.99 → v1.28.104 (2026-09-25, soir)** : (a) **routes dynamiques de l'export statique** : `public/_redirects` réécrivait aussi les pages liste (`/discord/polls/`, forms, events, tickets, backups, server/members|channels) vers la page « demo » ; corrigé avec des marqueurs `:id` (jamais sur une liste vide) au lieu de `/*`. Les sous-pages fixes (`create`, `compare`, `settings`) sont réécrites elles aussi vers la page modèle et **c'est `components/discord/ChildRouter.tsx`** (branché dans `[pollId]`, `[formId]`, `[eventId]`, `[backupId]`) qui affiche le bon écran d'après l'adresse. **Piège vérifié en production** : une règle vers la même adresse est ignorée, une règle vers `.../index.html` est redirigée puis retombe sur `:id` (comportement qui a changé d'un déploiement à l'autre) : ne pas en ajouter ; (b) **Sondages** : la création depuis le dashboard échouait toujours (le site n'envoyait pas d'`id` de question/option, types `WEIGHTED_VOTING`/`RANKED_CHOICE`/`SATISFACTION_RATING` et visibilité `AFTER_END` inconnus du bot, faux succès affiché). Le bot valide POST/PUT avec `DiscordPollSchema` (400 clair), le site suit les valeurs du bot, enchaîne créer → publier → déployer le panneau et n'annonce plus un succès simulé ; chiffres inventés retirés de la page liste ; test `test_polls_create_route_v1` ; vérifié en direct (création, panneau posté dans le salon) ; (c) **bug de suppression de messages** : Anti-Raid et AutoMod comptaient les messages **sans texte** (sondage natif Discord, image, autocollant) comme doublons (même hash de contenu vide) et les supprimaient + mettaient l'auteur en sourdine à la 3ᵉ occurrence ; corrigé (`raidDetectionService`, `spamDetector`), test dans `test_automod_infractions_v1` ; (d) **demande du propriétaire : tout désactivé sauf la musique sur tous les serveurs** : migration unique `2026-09-25-everything-off-except-music` (`services/moduleMigrations.ts`), appliquée au VPS à 21:38 sur les 2 serveurs où le bot est présent (`1157791788652839024`, `1128633164290596884`), sauvegarde préalable `~/backups/data-before-alloff-20260925-2138` sur le VPS ; le serveur `1088868377830891561` n'a plus le bot (sa config garde d'anciens interrupteurs : à couper si le bot y revient). Un module verrouillé (`own.pinned`, ex. rôles sécurisés en place) reste actif ; (e) **un module désactivé est maintenant vraiment inerte** : `events/messageCreate.ts` et `handlers/eventHandler.ts` (vocal) vérifient `isModuleEnabled` avant sticky, AFK, comptage, stats, highlights, Anti-Raid, AutoMod, XP, économie, IA, commandes personnalisées, salons vocaux temporaires (avant, seules les commandes étaient refusées pour les modules sans interrupteur propre). **Non gardé encore** (à vérifier si besoin) : suivi des invitations, planificateurs (giveaways, événements, rappels), starboard/réactions déjà gérés par leur propre interrupteur. **Vérifié en direct sur Discord (serveur de test)** : `/module`, `/giveaway start` + participation + tirage automatique, `/suggest` (refus propre sans salon), `/afk` + retour automatique, `/birthday set|remove`, `/poll list`, commande d'un module désactivé refusée avec l'embed « Module désactivé ». **Non testé** : tickets, bienvenue, backups, highlights, `/report user`, création de salon vocal.

## Reste à faire (par priorité)
1. **Latence au lancement d'une musique** (corrigée côté code en v1.28.70, à confirmer après redéploiement). Après redéploiement du bot, lancer `/play` et lire `pm2 logs ethone-bot | grep Lavalink` : les lignes de chronométrage disent si le temps part dans Spotify, la recherche Lavalink ou yt-dlp (`directYoutube`, timeout 25 s). Pistes : réponse immédiate `deferReply` puis « ajouté » sans attendre le flux ; lancer la préparation du flux du titre suivant en avance ; cache/pré-résolution côté `resolver.py` (options yt-dlp `player_client`, pas de format lourd) ; exécuter la recherche `ytmsearch` et `ytsearch` en parallèle plutôt qu'en séquence ; réutiliser le résultat de la recherche Spotify pour ne pas refaire une recherche texte dans `ensureEncoded`.
2. **Redéployer le bot** (rien de ce qui est côté bot n'agit tant que ce n'est pas fait) puis vérifier dans Chrome : sélecteur de serveur (choix qui change vraiment), page Accueil (activité du bon serveur), onglets du centre de contrôle (Diagnostics, Intégrations, Tâches, Incidents).
3. **Côté site du centre de contrôle** (`ethone-next/app/discord/bot/BotControlClient.tsx`, ~3000 lignes) : vérifier que chaque onglet gère bien les nouvelles réponses (liste d'intégrations variable, type `lavalink`, tâches à 0 exécution, `latency` à 0 → afficher « — », `version` du bot, ligne 284 contient encore `version: "2.4.0"` en dur à supprimer). Le module « Sécurité & Audit » et « Performance » ont été corrigés côté bot mais pas revérifiés visuellement.
3bis. **Paramètres du bot (`botConfigService.ts`, `GET/PUT /api/bot/settings`)** : valeurs par défaut en mémoire, jamais persistées ni appliquées (mode maintenance, niveau de log, rétention, limite de dépense IA, webhook d'alerte masqué en dur). Le PUT répond un succès alors que rien ne change au comportement du bot : soit persister et appliquer réellement (au moins `maintenanceMode` et `logLevel`), soit retirer l'écran. `POST /api/bot/restart` fait un `process.exit(0)` (pm2 relance) : réel.
4. **IA (`botAiMonitorService.ts`)** : le fournisseur/modèle affichés sont des libellés fixes, le coût est une estimation (répartition 50/50 prompt/completion, tarifs supposés), `successRate` vaut 100 % sans requête, et les compteurs « 24 h » ne sont jamais remis à zéro. À rendre honnête (afficher « estimation », « — » sans requête, vraie fenêtre glissante, vrai modèle utilisé).
5. **Boutons non testés en direct** : tous les boutons de toutes les pages n'ont pas été cliqués. Formulaires et sondages n'ont eu que le nettoyage des faux contenus. À faire : passe de tests en direct avec Chrome (serveur de test `1128633164290596884`).
6. **Plan non exécuté** (`C:\Users\storm\.claude\plans\vivid-percolating-quasar.md`, hors dépôt) : porte de vérification anti-bot à l'arrivée (`guildMemberAdd`), module économie « Crédits ETHONE », suivi d'habitudes (Supabase), refonte du centre de contrôle en 5 groupes. À confirmer avec l'utilisateur avant d'y toucher : certains points ont pu être faits entre-temps (vérifier le code).
7. **Fausses données possibles restantes** : refaire une passe `Math.random`, « mock », « demo », chiffres codés en dur dans `ethone-next/app/discord/**` et `discord-bot/src/server/routes/*` (le dernier balayage a porté sur `botControl/`, les routes et `statsService.ts`).
8. **Petites dettes** : `logger.error` alimente maintenant les incidents (vérifier qu'aucun module ne journalise en boucle des erreurs attendues, sinon bruit) ; avertissements CRLF/LF de git sur plusieurs fichiers (normaliser avec `.gitattributes`).

## Recommandations
- Toujours vérifier les changements du bot avec `npx tsc --noEmit` et les tests `test_*.ts` (onboarding 19, accueil 14, vocal 29, logs 21, musique 31 au moment de la passation) ; côté site : `npx tsc --noEmit`, `npx jest` (152 tests) et `npm run build`.
- `tsc` ne détecte pas qu'on sérialise une promesse (`res.json(promise)`) : après avoir rendu une fonction asynchrone, relire ses appelants.
- Les scripts de patch Python posés via l'outil d'écriture (et non en heredoc) évitent les pertes d'échappement ; faire les assertions avant d'écrire.
- Un secret collé dans le chat doit être refusé et à faire tourner (rotation) immédiatement.
